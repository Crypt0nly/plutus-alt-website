{
  "name": "plutus-alt-website",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "description": "PLUTUS — the AI assistant that does the work. A cinematic single-page site: aurora light, serif statements, a self-clearing notification pile, a draggable before/after Monday, real-product pricing and FAQ. Vite + vanilla JS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
