{
  "name": "plutus-alt-website",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "PLUTUS — the AI agent that actually does your work. A clean, Apple-style single-page marketing site built with Vite and vanilla JS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
